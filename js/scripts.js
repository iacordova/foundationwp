$(function(){

	// smooth scroll navigation
	$('nav ul').localScroll(800);

	// parallax with 0.1 speed
	$('.parallax-s01').parallax("50%", 0.1);

	// parallax with 0.2 speed
	$('.parallax-s02').parallax("50%", 0.2);	

	// parallax with 0.3 speed
	$('.parallax-s03').parallax("50%", 0.3);

});